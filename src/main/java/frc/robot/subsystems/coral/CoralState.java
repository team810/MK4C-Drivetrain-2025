package frc.robot.subsystems.coral;

public enum CoralState {
    Store, // This is for when there is no coral, and it just needs to be out of the way
    Hold, // When holding coral
    Intake, // Intake from source
    Score // Scoring on reef
}
